services:
  elec:
    build: .
    container_name: elec-meter
    ports:
      - "16543:16543"
    volumes:
      - elec-data:/app/data
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: "0.5"

volumes:
  elec-data:
